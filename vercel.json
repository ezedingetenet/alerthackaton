{
  "buildCommand": "npm install",
  "outputDirectory": "FrontEnd",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[...route]" }
  ]
}
